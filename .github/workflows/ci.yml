name: CI Orchestrator

on:
  push:
    branches: [ main, next ]
  pull_request:
    branches: [ main, next ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  typecheck:
    uses: ./.github/workflows/typecheck.yml

  test:
    uses: ./.github/workflows/test.yml

  coverage:
    uses: ./.github/workflows/coverage.yml

  emotionArc:
    uses: ./.github/workflows/emotionArc.yml

  validate-mcp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm ci
      - name: Validate MCP Config
        run: node scripts/validate-mcp-config.js

  audit-export:
    uses: ./.github/workflows/export-audit-logs.yml
    secrets: inherit
    with:
      run_audit: ${{ secrets.AWS_ACCESS_KEY_ID != '' || secrets.GCP_SERVICE_ACCOUNT != '' }}

  audit-reingest:
    uses: ./.github/workflows/reingest-audit-logs.yml
    secrets: inherit
    with:
      run_audit: ${{ secrets.AWS_ACCESS_KEY_ID != '' || secrets.GCP_SERVICE_ACCOUNT != '' }}

  summary:
    runs-on: ubuntu-latest
    needs: [lint, typecheck, test, coverage, emotionArc, validate-mcp, audit-export, audit-reingest]
    if: always()
    steps:
      - name: Check workflow status
        run: |
          echo "Checking status of all modular workflows..."
          
          CORE_FAILURE=false
          
          if [[ "${{ needs.lint.result }}" != "success" ||
                "${{ needs.typecheck.result }}" != "success" ||
                "${{ needs.test.result }}" != "success" ||
                "${{ needs.coverage.result }}" != "success" ||
                "${{ needs.emotionArc.result }}" != "success" ||
                "${{ needs.validate-mcp.result }}" != "success" ]]; then
            CORE_FAILURE=true
          fi
          
          if [[ "$CORE_FAILURE" == "true" ]]; then
            echo "❌ Some core CI checks failed:"
            echo "❌ Lint: ${{ needs.lint.result }}"
            echo "❌ Typecheck: ${{ needs.typecheck.result }}"
            echo "❌ Test: ${{ needs.test.result }}"
            echo "❌ Coverage: ${{ needs.coverage.result }}"
            echo "❌ EmotionArc: ${{ needs.emotionArc.result }}"
            echo "❌ MCP Validation: ${{ needs.validate-mcp.result }}"
            exit 1
          else
            echo "✅ All core CI checks passed successfully!"
            echo "✅ Lint: ${{ needs.lint.result }}"
            echo "✅ Typecheck: ${{ needs.typecheck.result }}"
            echo "✅ Test: ${{ needs.test.result }}"
            echo "✅ Coverage: ${{ needs.coverage.result }}"
            echo "✅ EmotionArc: ${{ needs.emotionArc.result }}"
            echo "✅ MCP Validation: ${{ needs.validate-mcp.result }}"
          fi
          
          echo "📊 Audit Export: ${{ needs.audit-export.result }}"
          echo "📊 Audit Reingest: ${{ needs.audit-reingest.result }}"

      - name: Comment on PR with summary
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const lintStatus = '${{ needs.lint.result }}' === 'success' ? '✅' : '❌';
            const typecheckStatus = '${{ needs.typecheck.result }}' === 'success' ? '✅' : '❌';
            const testStatus = '${{ needs.test.result }}' === 'success' ? '✅' : '❌';
            const coverageStatus = '${{ needs.coverage.result }}' === 'success' ? '✅' : '❌';
            const emotionArcStatus = '${{ needs.emotionArc.result }}' === 'success' ? '✅' : '❌';
            const mcpValidationStatus = '${{ needs.validate-mcp.result }}' === 'success' ? '✅' : '❌';
            const auditExportStatus = '${{ needs.audit-export.result }}' === 'success' ? '✅' : '📊';
            const auditReingestStatus = '${{ needs.audit-reingest.result }}' === 'success' ? '✅' : '📊';

            const overallStatus =
              '${{ needs.lint.result }}' === 'success' &&
              '${{ needs.typecheck.result }}' === 'success' &&
              '${{ needs.test.result }}' === 'success' &&
              '${{ needs.coverage.result }}' === 'success' &&
              '${{ needs.emotionArc.result }}' === 'success' &&
              '${{ needs.validate-mcp.result }}' === 'success'
                ? '✅ PASSED'
                : '❌ FAILED';

            // Fetch all jobs for this workflow run
            const { data: jobs } = await github.rest.actions.listJobsForWorkflowRun({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: context.runId
            });

            function linkForJob(name) {
              const job = jobs.jobs.find(j => j.name.toLowerCase().includes(name.toLowerCase()));
              return job ? `[${job.name}](${job.html_url})` : `${name} (link not found)`;
            }

            const jobLinks = 
              '### 🔗 Job Logs\n' +
              '- ' + linkForJob('lint') + '\n' +
              '- ' + linkForJob('typecheck') + '\n' +
              '- ' + linkForJob('test') + '\n' +
              '- ' + linkForJob('coverage') + '\n' +
              '- ' + linkForJob('emotionArc') + '\n' +
              '- ' + linkForJob('validate-mcp') + '\n' +
              '- ' + linkForJob('audit-export') + '\n' +
              '- ' + linkForJob('audit-reingest') + '\n';

            const comment = 
              '## CI Orchestrator Summary\n\n' +
              '**Overall Status:** ' + overallStatus + '\n\n' +
              '### Core Checks\n' +
              '- Lint: ' + lintStatus + '\n' +
              '- Type Check: ' + typecheckStatus + '\n' +
              '- Tests: ' + testStatus + '\n' +
              '- Coverage: ' + coverageStatus + '\n' +
              '- Emotion Arc: ' + emotionArcStatus + '\n' +
              '- MCP Validation: ' + mcpValidationStatus + '\n\n' +
              '### Audit Operations (Informational)\n' +
              '- Export Logs: ' + auditExportStatus + '\n' +
              '- Reingest Logs: ' + auditReingestStatus + '\n\n' +
              jobLinks + '\n' +
              '### Legend\n' +
              '- ✅ = Passed\n' +
              '- ❌ = Failed\n' +
              '- 📊 = Informational (non-blocking)\n\n' +
              '*Generated by GitHub Actions*';

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            }); 